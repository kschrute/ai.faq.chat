import type React from "react";

const Header: React.FC = () => {
  return (
    <h1 className="mb-10 text-5xl font-bold text-center bg-gradient-to-r from-cyan-400 to-blue-400 text-transparent bg-clip-text drop-shadow-[0_4px_8px_rgba(0,0,0,0.4)]">
      FAQ Chat
    </h1>
  );
};

export default Header;
