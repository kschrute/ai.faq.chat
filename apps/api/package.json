{
  "name": "@ai.faq/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "uv run --env-file .env.production python embed.py",
    "dev": "DEV=1 uv run --env-file .env.production uvicorn api:app --host 0.0.0.0 --port 8000 --reload"
  }
}
