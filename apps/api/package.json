{
  "name": "@ai.faq/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "uv run python embed.py",
    "dev": "uv run uvicorn api:app --host 0.0.0.0 --port 8000 --reload"
  }
}
